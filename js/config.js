/**
 * Site configuration file
 * Contains all pages and Menu Items
 * Will automatically generate the menu and link to [name].md in the markdown folder
 */

// Pages array with display names (first entry is the page ID, second is the display name)
const pages = [
  ['home', 'Home'],
  ['about', 'About Us'],
  ['menu', 'Menu'],
  ['contact', 'Contact Us']
];

